/* 横向滚动容器：给最后一列留出空间，避免删除按钮被裁 */
.table-scroll{
  width: 100%;
  overflow: hidden;
  padding-right: 40rpx;   /* ✅ 关键：右侧留白 */
  box-sizing: border-box;
}

/* 表格整体：强制撑开宽度，不要被压缩 */
.table{
  display: inline-block;  /* ✅ 关键：用 inline-block 让宽度按内容撑开 */
  min-width: 100%;
}

/* 行：用 flex，且禁止换行 */
.tr{
  display: flex;
  flex-direction: row;
  align-items: center;
  white-space: nowrap;
}

/* 单元格：禁止被压缩，避免错位 */
.td{
  flex-shrink: 0;         /* ✅ 关键：不允许缩小 */
  padding: 16rpx 12rpx;
  box-sizing: border-box;
  border-bottom: 1rpx solid #f2f2f2;
}

/* 表头 */
.th .td{
  font-weight: 600;
  background: #fafafa;
}

/* 各列宽度（你现在是 4 列：openid / 备注 / 角色 / 操作） */
.w-openid{ width: 520rpx; }
.w-remark{ width: 280rpx; }
.w-role{   width: 220rpx; }

/* 操作列要足够宽，否则“删除”会被挤掉 */
.w-op{
  width: 260rpx;          /* ✅ 关键：加宽 */
  padding-right: 30rpx;   /* ✅ 关键：再给操作列一点右内边距 */
}

/* 备注输入框不要撑爆 td */
.remark-input{
  width: 240rpx;
  box-sizing: border-box;
  border: 1rpx solid #eee;
  border-radius: 10rpx;
  padding: 8rpx 10rpx;
  font-size: 24rpx;
}